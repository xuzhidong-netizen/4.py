# ZTE iCenter-inspired workspace page

Static landing page modeled as a high-fidelity enterprise digital workspace.

## Run locally

Open `index.html` directly in a browser.

## Deploy

Push to `main`. GitHub Actions in `.github/workflows/deploy.yml` deploys the site to GitHub Pages.
